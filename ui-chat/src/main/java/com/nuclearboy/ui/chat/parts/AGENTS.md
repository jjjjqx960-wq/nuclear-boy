## 目录职责

`parts` 是 `ui-chat` 的零件层，承载聊天 UI 可复用的最小数据结构、常量和纯函数，例如命令元数据、滚动状态判断、工具受限提示识别、工具型草稿预警识别、防假执行约束去重追加、发送后工具能力证据提示文案、工具型请求后置结果复核文案、后置复核结构化提示识别、本轮模型真实性约束文案、文件面板过滤规则、当前显示列表规则、过滤摘要文案、过滤摘要范围标签、过滤清除语义描述、空结果文案、清除过滤动作显示条件、匹配引用过滤复位、匹配引用关闭规则、批量引用提示文案、批量引用大小反馈、单文件引用相对路径反馈、单文件引用大小反馈、文件面板摘要计算、摘要胶囊语义描述、文件面板排序规则、排序模式语义描述、可见文件选择、全选/补选数量标签、只看/全部切换数量标签、当前可见引用标签、可见文件取消选择、图标操作数量语义、已引用路径移除、取消匹配选择显示条件、引用匹配显示条件、引用按钮数量标签、隐藏选择清理、已选文件筛选、文件选择大小汇总、无已选可见总大小文案、选择操作条显示条件、选择状态文案、只看已选匹配状态、隐藏已选提示和文件引用提示生成。

## 边界

这里只描述稳定的小能力，不组合 Compose 布局，不依赖 ViewModel、Agent、文件系统或网络。

## 允许依赖

允许依赖 Kotlin 标准库和同层纯数据类型。

## 禁止事项

不要在零件层读取 Android Context、持久化文件、调用模型接口或直接渲染界面。

## 常用命令

- `./gradlew :ui-chat:compileDebugKotlin`
- `./gradlew :ui-chat:testDebugUnitTest`

## 验证方式

优先验证纯函数输入输出，包括空输入、工具受限识别、工具型草稿预警识别、防假执行约束追加和去重、发送后工具能力证据提示、工具型请求后置结果复核、后置复核结构化提示识别、本轮模型真实性约束、普通聊天不误判、普通错误不误判、大小写、扩展名、路径匹配、当前显示列表规则、过滤摘要文案、过滤摘要范围标签、过滤清除语义描述、空结果文案、清除过滤动作显示条件、匹配引用过滤复位、匹配引用关闭规则、批量引用提示文案、批量引用大小反馈、单文件引用相对路径反馈、单文件引用大小反馈、目录/文件计数、类型分布、摘要胶囊语义描述、排序优先级、排序模式语义描述、选择切换、可见文件全选、全选/补选数量标签、只看/全部切换数量标签、当前可见引用标签、可见文件取消选择、图标操作数量语义、已引用路径移除、取消匹配选择显示条件、引用匹配显示条件、引用按钮数量标签、隐藏选择清理、已选文件筛选、文件选择大小汇总、无已选可见总大小文案、选择操作条显示条件、选择状态文案、只看已选匹配状态、隐藏已选提示和引用提示格式，再由组件层和部件层编译验证接入效果。
