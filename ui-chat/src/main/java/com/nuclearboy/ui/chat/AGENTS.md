## 目录职责

`chat` 是 `ui-chat` 的部件层源码目录，承载聊天屏幕、聊天 ViewModel、流式错误收敛、消息渲染、工具受限状态呈现、工具型草稿预警、输入区防假执行约束追加、发送后工具能力证据提示、工具型请求后置结果复核及其结构化警告卡、本轮模型真实性约束注入、用户旅程可定位语义、Token HUD 和文件面板等用户聊天工作流。

## 边界

这里可以组合组件层与下游业务模块形成完整聊天体验，但新增可复用数据和局部 UI 应先下沉到 `parts` 或 `components`。

## 允许依赖

允许依赖 `components`、`parts`、`common`、`api-deepseek`、`agent-core`、`skills`、`tools-docgen`、`memory` 和 Compose/Hilt 相关库。

## 禁止事项

不要在 UI 文案、日志或系统消息中复述明文 Token、密码、签名密钥或完整个人数据。不要把模型协议、文件系统细节或工具执行逻辑写进屏幕组件。

## 常用命令

- `./gradlew :ui-chat:compileDebugKotlin`
- `./gradlew :app:assembleDebug`

## 验证方式

重点验证普通发送、聊天输入与发送按钮语义、工具型草稿预警、发送后工具能力证据提示、工具型请求后置结果复核、后置复核结构化警告卡、本轮模型真实性约束、快捷命令、`/goal`、`/loop`、`/stop`、`/compact`、`/rewind`、消息持久化、流式状态更新、流式错误气泡收敛、工具受限提示卡呈现，以及文件面板过滤、只看已选范围摘要、无匹配清除过滤入口、摘要、只看已选过滤摘要、只看已选列表过滤、只看已选匹配状态、全选/补选数量标签、只看/全部切换数量标签、图标操作数量语义、无已选可见总大小提示、当前可见直接引用、取消匹配选择、引用匹配、引用按钮数量标签、匹配引用后保留未引用选择、匹配引用后过滤复位、单文件引用相对路径反馈、单文件引用大小反馈、批量引用大小反馈、批量引用剩余数量提示、最后一批匹配引用自动收起、排序、单选引用、批量引用、隐藏选择清理、无匹配隐藏选择操作条、预览和引用文件。
