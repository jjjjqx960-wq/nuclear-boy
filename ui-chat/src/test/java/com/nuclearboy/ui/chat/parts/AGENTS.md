## 目录职责

这里承载 `ui-chat` 零件层的 JVM 单元测试，重点覆盖正式聊天失败诊断识别、工具受限提示识别、工具型草稿预警识别、API/接口调用和远程配置类草稿识别、后台/网关/服务端自然语言配置草稿识别、API 专属不可执行说明与证据目标、防假执行约束追加和去重、发送后工具能力证据提示、工具型请求后置结果复核、后置复核结构化提示识别、本轮模型真实性约束、文件面板过滤、当前显示列表规则、过滤摘要、过滤摘要范围标签、过滤清除语义描述、空结果文案、清除过滤动作显示条件、匹配引用过滤复位、匹配引用关闭规则、批量引用提示文案、批量引用大小反馈、单文件引用相对路径反馈、单文件引用大小反馈、排序、排序模式语义描述、摘要、摘要胶囊语义描述、选择、全选可见、全选/补选数量标签、只看/全部切换数量标签、当前可见引用标签、取消可见、图标操作数量语义、已引用路径移除、取消匹配选择显示条件、引用匹配显示条件、引用按钮数量标签、隐藏选择清理、只看已选、文件选择大小汇总、无已选可见总大小文案、选择操作条显示条件、选择状态文案、只看已选匹配状态、隐藏已选提示、引用提示和聊天输入辅助等纯函数。

## 边界

测试应只验证 `parts` 层稳定规则，不启动 Compose、不访问 ViewModel、不读写真实文件系统。

## 允许依赖

允许依赖 `ui-chat/src/main/.../parts`、`common` 模型、JUnit 和 Kotlin 标准库。

## 禁止事项

不要在测试中复述或构造真实账号、Token、密钥和个人数据。不要把 UI 布局断言写入零件层测试。

## 常用命令

- `./gradlew :ui-chat:testDebugUnitTest`

## 验证方式

用小型内存数据覆盖正式聊天失败诊断识别、工具受限提示识别、工具型草稿预警识别、发送后工具能力证据提示、工具型请求后置结果复核、后置复核结构化提示识别、普通聊天不误判、普通错误不误判、排序优先级、排序模式语义描述、摘要胶囊语义描述、大小写、空扩展名、目录优先、数量统计、文件面板过滤、当前显示列表规则、过滤摘要、过滤摘要范围标签、过滤清除语义描述、空结果文案、清除过滤动作显示条件、匹配引用过滤复位、匹配引用关闭规则、批量引用提示文案、批量引用大小反馈、单文件引用相对路径反馈、单文件引用大小反馈、选择切换、全选可见、全选/补选数量标签、只看/全部切换数量标签、当前可见引用标签、取消可见、图标操作数量语义、已引用路径移除、取消匹配选择显示条件、引用匹配显示条件、引用按钮数量标签、隐藏选择清理、只看已选、文件选择大小汇总、无已选可见总大小文案、选择操作条显示条件、选择状态文案、只看已选匹配状态、隐藏已选提示和提示格式等规则。
