# NUCLEAR BOY (核弹男孩)

> 温暖、智能、人性化的移动端 AI 编程助手 · v1.0.79

---

## ⚠️ 声明

**本仓库非原作者上传。**

- **原作者**: [mzpr00](https://github.com/mzpr00) (穆再排尔·穆合塔尔)
- **本仓库维护者**: [jjjjqx960-wq](https://github.com/jjjjqx960-wq)
- **说明**: 本仓库为原项目的复制/镜像（mirror），仅供学习研究使用。所有代码版权归原作者 mzpr00 所有。

---

## 项目简介

Nuclear Boy 是一个基于 Android 的移动端 AI 编程助手，内置 Agent 引擎、工具注册、Python 执行器、三层记忆系统等能力。

## 技术栈

- **UI**: Jetpack Compose + Material 3 (暖色主题 #FF8C42)
- **DI**: Hilt (SingletonComponent)
- **网络**: OkHttp 4 + 流式 SSE
- **数据库**: Room (SQLite)
- **Python**: Chaquopy 15 (嵌入式 CPython)
- **序列化**: kotlinx.serialization

## 构建

1. 安装 Android Studio + SDK 35
2. 设置 `ANDROID_HOME` 环境变量
3. `./gradlew assembleDebug`

## 1.0.79 重点

- 接入 OpenCode（1.17.4）：pc_cli_run 现支持 claude / codex / opencode 三大主流编程 CLI，opencode 走 run --format json 事件流，-s 会话续传。
- bridge 配置非破坏性迁移：默认配置新增的 CLI 自动合并进旧 config.json。
- 实测：opencode 建会话 6s、续传答出暗号 5.7s（三者中最快）；真机远程任务 6.9s 回传。

## 1.0.78 重点

- session="last"：续传不再需要复制会话 ID，AI 直接传 last 就接上该 CLI 最近一次任务的上下文（按 CLI 分别记忆、加密落盘、重启 App 仍有效），真机实测通过。
- 全量自检新增"远程电脑桥接"检查项：未开启给提示、未配置给指引、已配置实测连通并列出可用 CLI。
- bridge 安全加固：同一来源连续 5 次鉴权失败封禁 5 分钟（实测第 6 次起连正确 token 也被拒），防 token 爆破。
- README 新增远程电脑使用指南（电脑端三条命令 + 手机端配置 + 常用说法）。

## 远程电脑使用指南

让手机上的核弹男孩控制电脑上的 Claude Code / Codex：

1. **电脑端**（一次性）：
   ```bash
   cd D:	ools
b-pc-bridge
   python bridge.py init                # 生成 token，记下显示的 ws:// 地址
   python bridge.py install-autostart   # 注册登录自启（可选）
   python bridge.py serve               # 或手动启动
   ```
2. **手机端**：设置 → 远程电脑 → 打开开关，填电脑地址（如 `ws://192.168.1.10:7860`）和 token → 测试连接。
3. **使用**：直接对核弹男孩说——
   - "让电脑上的 claude 修复 D:/myproject 的编译错误"
   - "用 codex 在电脑上写个脚本"（自动返回会话 ID）
   - "继续刚才那个任务，再加上单元测试"（AI 用 session=last 续传）
   - "电脑上在跑什么？"（pc_task_list）
   - 实验性改动可要求"隔离执行"（worktree，不碰主工作区）

特性：任务输出实时流到聊天工具卡片；手机断线任务不丢（重连自动取回）；取消对话自动终止电脑任务；鉴权失败 5 次封禁 5 分钟防爆破。

## 1.0.77 重点

- worktree 隔离执行（借鉴 ccpocket）：pc_cli_run 传 isolate=true 时在 git 仓库旁创建独立 worktree + nb/ 分支执行，改动不碰主工作区，结果返回 worktree 路径；适合实验性修改和并行多任务。
- bridge 新增 clean-worktrees --repo 一键清理 nb/ worktree 和分支。
- 设置页"远程电脑"新增"查看电脑任务"按钮，直接看正在执行的任务列表。
- 实测：claude 真实在 worktree 写文件、主仓库零污染、清理命令生效；3 并发任务（队列上限 2）全部正确完成。

## 1.0.76 重点

- 取消联动：用户在手机上取消对话时，自动通知电脑端终止正在执行的远程任务，不再白烧 token。
- 新增 pc_task_list 工具：AI 可回答"电脑上在跑什么"，显示任务 ID、CLI、已运行时长和任务摘要。
- 电脑端 bridge 新增 install-autostart/uninstall-autostart：注册 Windows 登录自启计划任务（pythonw 后台 + bridge.log 日志），电脑重启后手机随时能连。
- 桥接协议新增 list_tasks/cancelled 消息；取消、列表、取消后移除、未知任务报错全路径实测通过。

## 1.0.75 重点

- 远程任务执行过程实时可见：pc_cli_run 跑任务时，电脑端 claude/codex 的输出实时流到聊天界面的工具卡片——收起时显示最新一行进度，点开看完整滚动日志（保留最近 4000 字符）。
- 新增 common 层 ToolProgressBus 进度总线，任何长耗时工具都可以接入实时进度，UI 与工具实现解耦。
- 借鉴 happy（21.8k 星）的工具调用生命周期卡片渲染思路（tool-call-start/end + 增量更新）。

## 1.0.74 重点

- codex 也支持会话续传：捕获 thread_id 并走 codex exec resume，双 CLI 续传能力对齐。
- 断线不丢任务：手机网络波动断线时，电脑端任务继续后台执行并缓存结果（最近 100 条）；手机端自动重连（最多 5 次），任务执行中则重挂输出流，已完成则直接取回结果。
- 真机实测：codex 跨任务记忆通过；模拟断线后重连取回结果通过；缓存取回与未知任务报错路径全部验证。

## 1.0.73 重点

- 远程电脑任务支持会话续传：pc_cli_run 结果返回会话 ID，下次传入 session 参数即可让电脑端 claude 带着之前的上下文继续干活，适合"修完再改、改完再问"的连续迭代。
- 方案借鉴同赛道开源项目 ccpocket（870 星）和 cc-connect 的 claude --resume 实践，参考库归档于 D:
b-reference。
- 真机实测：第一个任务记下暗号、第二个任务续传会话准确答出，跨任务记忆 10.8s 回传。

## 1.0.72 重点

- 新增"远程电脑"能力：在设置页配置电脑地址和 token 后，AI 可通过 pc_cli_run 把编程任务下发给电脑上的 Claude Code / Codex 执行，结果流式回传手机。
- 电脑端配套 nb-pc-bridge 守护进程（Python WebSocket 服务，token 鉴权，hmac 时序安全比对），支持局域网/USB 共享网络直连，协议预留自建中继扩展。
- 新增 pc_bridge_status 工具，AI 可自查电脑连接状态和可用 CLI 版本。
- 新增 DEBUG_SAVE_PC_BRIDGE 调试广播，ADB 一条命令写入配置并实跑任务验证。
- 真机实测：USB 直连下手机下发 claude 任务 11 秒回传结果，token 错误正确拒绝，全模块单测和聊天旅程门禁通过。

## 1.0.70 重点

- 修复正式聊天用户消息重复发送、空 placeholder 混入历史的问题，正式聊天 prompt 更干净省 token。
- 按官方文档统一剥离 reasoning_content，官方 DeepSeek 多轮思考模式不再触发 400。
- 修复记忆数据库每次打开失败的问题，三层记忆系统（用户画像、对话计数、语义记忆）恢复可用。
- 流式中断不再覆盖已输出的部分回复；修复 OkHttp 连接泄漏和取消异常被吞。
- 页面显示修复：文件面板/输入栏不透明消除文字鬼影、附件角标不再误报、内部目录不再泄漏到文件面板。
- 第三方模型激活时隐藏官方专属的聊天/思考/专家模式和"模型偏好"设置，只展示真实生效的配置。
- 旅程测试支持 root 设备亮屏解锁、shell 注入和 instrumentation 参数预置网关，已在真机全链路跑通。

## 1.0.69 重点

- 新增设备端真实聊天 UI 旅程测试，发布前可用 UIAutomator 启动 App、输入消息、点击发送并等待正式聊天完成。
- 聊天输入框和发送按钮补充稳定无障碍语义，方便真实前端测试和辅助操作定位。
- 发布门禁从“构建和诊断通过”升级为“真实用户路径可操作、无空回复和误导错误”。

## 1.0.68 重点

- 第三方 OpenAI 兼容模型正式聊天时，如果网关拒绝工具调用格式，会自动切换到兼容聊天模式重试。
- 自定义网关请求会清理历史消息里的 DeepSeek reasoning 扩展字段，降低“测试能通但聊天 400/404/422”的概率。
- Debug 诊断新增第三方正式聊天兼容性检查，覆盖 `stream=true` 和工具定义，避免只测 ping 的盲区。
- 流式请求报错时，聊天气泡直接显示具体错误，不再叠加“没能生成回复”的空白结果。

## 1.0.67 重点

- 第三方模型测试结果的复制按钮升级为“测试排障包”，自动带上测试结果、请求上下文和脱敏 POST curl。
- 新增模型表单如果已经获取过模型列表，排障包会附带模型列表摘要和脱敏 GET `/v1/models` 模板。
- 用户遇到 404、鉴权失败或网关路由异常时，可以一次复制完整上下文给网关后台或对照抓包，不需要手动拼信息。

## 1.0.66 重点

- 添加第三方模型时，设置页会根据服务地址和模型名生成更短的推荐显示名。
- 例如 `http://154.12.90.249:20128/v1` + `nvidia/minimaxai/minimax-m2.7` 会推荐 `154.12.90.249 minimax-m2.7`。
- 显示名称为空时可一键填入推荐名，保存后的模型列表更容易区分不同网关和模型。

## 1.0.65 重点

- 获取模型列表后，如果当前 `nvidia/...` 模型名不在列表里，设置页会自动推荐最可能的列表候选。
- 候选匹配优先使用去掉 provider 前缀后的完整后缀，再回退到最后一段模型名，例如从 `nvidia/minimaxai/minimax-m2.7` 推荐 `minimaxai/minimax-m2.7` 或 `minimax-m2.7`。
- 用户可以一键“改用列表候选”，不需要在长模型列表里手动找相似名称。

## 1.0.64 重点

- 添加第三方模型时，模型名输入框会提前提示 `nvidia/...` 这类 provider 前缀的路由风险。
- 如果还没获取模型列表，会提示先拉取并点选网关实际可用模型名；如果列表已拉取，会区分“精确命中”和“列表未包含”。
- 这样用户在测试前就能判断是模型名不在网关列表，还是网关上游凭证/额度问题，减少 404 反复试错。

## 1.0.63 重点

- 模型列表探测结果区新增“复制模型列表请求模板”。
- 模板按当前 `/v1/models` endpoint 和 Key 是否填写生成脱敏 GET curl，便于复现模型列表接口。
- 适合把列表探测请求发给网关运维或和抓包结果对齐，Authorization 只输出 `<REDACTED_TOKEN>`。

## 1.0.62 重点

- 第三方模型表单会在实际请求预览下方提供“复制请求模板”。
- 模板按当前协议、地址模式、服务地址和模型名生成最小 ping curl，方便和抓包、ccswitch 或网关日志对齐。
- Authorization 只输出 `<REDACTED_TOKEN>` 占位符，不复制明文 Key。

## 1.0.61 重点

- 获取模型列表后，如果当前模型名把候选过滤为空，会出现“查看全部模型”按钮。
- 点击后自动清空模型名筛选并保留已获取的模型列表，不需要手动删除错误模型名。
- 适合 404 后从错误模型名快速回到网关实际可用模型，点选后即可重新测试或保存。

## 1.0.60 重点

- 第三方模型测试失败时，会在技术错误前显示可执行的“操作建议”。
- 对 provider 凭证缺失、模型不存在、鉴权失败、权限不足、网络不可达分别给出下一步排障路径。
- 复制测试摘要时同步带上操作建议，方便直接发给网关运维核对，不暴露明文 Key。

## 1.0.59 重点

- 获取第三方模型列表后，模型名输入框会作为筛选关键词实时过滤可点选模型。
- 大网关返回几十上百个模型时，不再只能从前 12 个样例里找；输入 `mini`、`claude`、`qwen` 等关键词即可缩小候选。
- 列表提示会显示当前展示数量、总数和无匹配原因，减少反复复制模型名试错。

## 1.0.58 重点

- 添加第三方 OpenAI 兼容模型时，可以先点“获取模型列表”主动请求 `/v1/models`。
- 成功后设置页直接显示模型数量、探测 endpoint、模型名样例，并支持点选模型名自动填入输入框。
- 模型列表摘要可一键复制，方便把可用模型名发给网关运维或和 ccswitch 配置对齐。

## 1.0.57 重点

- 第三方 OpenAI 兼容模型测试遇到 404 时，会自动 GET 对应网关的 `/v1/models` 做一次模型列表探测。
- 失败详情会显示模型列表探测是否成功、解析到的模型数量，以及最多 12 个可用模型名样例。
- 探测结果只输出模型 id 和脱敏错误片段，不输出 API Key 或授权头，方便直接截图给网关运维核对。

## 1.0.56 重点

- 第三方模型服务地址会像模型名一样自动清理零宽、BOM 和控制类隐藏字符。
- 添加模型表单会即时显示清理后的实际请求地址，测试、保存、端点预览和一键补全都使用同一结果。
- 已保存的旧配置和 Debug 配置入口也会在读取/测试时兜底清理，减少复制粘贴导致的不可见 URL 问题。

## 1.0.55 重点

- 完整地址模式出现非完整接口风险时，新增“一键补成完整地址”按钮。
- 点击后会保留完整地址模式，并把服务地址直接改成自动拼接出的最终接口 URL。
- 用户可以在“补成完整地址”和“切回智能拼接”之间选择，更贴合不同网关配置习惯。

## 1.0.54 重点

- 完整地址模式出现非完整接口风险时，提示下方新增“一键切回智能拼接”按钮。
- 点击后地址模式立即回到智能拼接，实际请求预览同步刷新到标准接口路径。
- 这个动作只在风险提示存在时显示，已填写正确完整接口的用户不会被额外打扰。

## 1.0.53 重点

- 第三方模型测试失败时，错误详情顶部会显示本次实际请求上下文。
- 上下文包含实际端点、模型名、解析后的协议、地址模式和 Key 脱敏指纹，便于和网关日志逐项对照。
- 测试中、成功和失败状态复用同一套上下文格式，避免排障截图和复制摘要信息不一致。

## 1.0.52 重点

- 完整地址模式下，如果实际请求 URL 不像完整接口，会直接提示风险。
- OpenAI 兼容模式会提示应填写 `/v1/chat/completions` 结尾的完整 URL，或切回智能拼接。
- 正确的 OpenAI/Anthropic 完整接口不提示，避免干扰已经配置正确的用户。

## 1.0.51 重点

- 添加第三方模型时会实时预览实际请求端点，例如 `POST .../v1/chat/completions`。
- 预览复用真实发包的 OpenAI/Anthropic 协议和地址模式构造逻辑，减少地址模式选错导致的 404。
- 空地址不显示预览，避免未填写时产生干扰。

## 1.0.50 重点

- 添加第三方模型时，模型名输入框会即时提示隐藏字符清理后的实际请求模型名。
- 测试填写和添加并选中都按清理后的模型名启用与提交，避免只粘贴不可见字符也能触发操作。
- 普通首尾空格不会触发隐藏字符提示，减少正常复制粘贴时的干扰。

## 1.0.49 重点

- 全量自检结果新增复制摘要按钮，可一键复制总数、失败/警告数量、每项状态、耗时和详情。
- 摘要会自动脱敏 `Bearer` 和 `sk-` Token，方便把完整自检结果转发给运维排查。
- 复制格式下沉到设置页零件层并补单元测试，避免全量自检和模型测试的脱敏规则分叉。

## 1.0.48 重点

- 第三方模型测试结果框新增复制摘要按钮，可一键复制当前测试状态、标题和详情。
- 复制内容会脱敏 `Bearer` 和 `sk-` Token，方便转发给服务端运维或对照 9Router 日志。
- 成功、失败和测试中状态都生成同一格式摘要，减少手动整理 404、超时和模型路由问题的时间。

## 1.0.47 重点

- 第三方模型测试会在模型名被自动清理隐藏字符时显示“实际请求使用”的模型名。
- 用户复制粘贴带零宽字符的模型名时，不再只有静默修复；测试中、成功和失败详情都会保留可核对提示。
- 设置页提示复用底层同一个模型名清理规则，避免 UI 显示和真实请求不一致。

## 1.0.46 重点

- 第三方模型名保存、读取、测试和真实发包前都会清理零宽隐藏字符。
- 解决复制粘贴导致 `nvidia/minimaxai/minimax-m2.7` 前缀被网关识别成隐藏字符+nvidia，从而返回 `No active credentials for provider` 的 404。
- 已保存的旧异常模型名读取时会被兜底修正，不需要用户手动删除重建。

## 1.0.45 重点

- Debug 包新增 ADB/API 写入第三方模型配置入口，可直接把本机 ccswitch/crush 生效模型写入手机 App。
- 写入后会立即选中该模型，日志只记录状态、协议、地址模式和 Key 长度，不输出明文 Key。
- 入口只在 debug manifest 注册，release 包不会暴露这个调试写配置能力。

## 1.0.44 重点

- 第三方模型 API Key 输入框会显示脱敏指纹和长度，例如 `sha256 ba7816bf8f01 · 3 位`，不显示明文。
- 模型测试进行中和成功详情也会带同一指纹，方便对照 ccswitch/crush 生效凭证是否一致。
- 指纹计算下沉到设置页零件层，测试覆盖 sha256 前缀、输入裁剪和空值兜底。

## 1.0.43 重点

- 第三方模型测试失败标题会识别 `No active credentials for provider`，直接显示“网关缺少 nvidia 上游凭证”。
- `model_not_found` 等模型路由错误会显示“模型路由失败，请核对模型名”，不再误报为请求格式错误。
- 对 provider 凭证短暂不可用会自动短重试一次，贴近 ccswitch 代理层的实际行为；详情区仍保留 HTTP 状态、端点排查和返回片段。

## 1.0.42 重点

- 第三方模型测试遇到 OpenAI 兼容 `404` 时，会提示先带 Key 请求 `/v1/models` 核对真实可用模型名。
- 对标准 `/v1/chat/completions` 的 404，不再只引导改地址，而是提示检查网关上游 provider 凭证和模型路由。
- 对 NVIDIA MiniMax 这类模型补充前缀提示：网关列表没有 `nvidia/` 时，可尝试 `minimaxai/minimax-m2.7`。

## 1.0.41 重点

- 文件面板摘要条的图标语义会带上实际数值，例如“目录 3”“文件 12”“大小 4 KB”。
- 用户使用读屏、长按语义或辅助操作时，不必逐个读取旁边文本也能获得当前结果规模。
- 摘要胶囊语义文案下沉到零件层，组件层只负责接入，摘要计算和布局保持不变。

## 1.0.40 重点

- 文件面板排序按钮的语义描述会区分当前排序和可切换目标，例如“当前按名称排序”“切换为按大小排序”。
- 用户使用读屏、长按语义或辅助操作时，能判断当前排序状态和点击后的结果。
- 排序语义文案下沉到零件层，组件层只负责接入，排序算法和布局保持不变。

## 1.0.39 重点

- 文件面板进入“只看已选”后，搜索框摘要会切换到已选范围，例如“已选 1 / 2”。
- 复核大量已选文件时，用户能从搜索框直接判断当前匹配的是已选集合，而不是误看成全目录结果。
- 范围摘要规则下沉到零件层，部件层只根据当前模式传入全目录或已选范围。

## 1.0.38 重点

- 文件面板过滤无匹配时，空结果区域会直接显示“清除过滤”按钮，不必回到搜索框尾部清除。
- 空状态文案会显示规整后的过滤词，用户能确认当前没有命中的是哪个关键字。
- 空结果文案和清除动作显示条件下沉到零件层，组件层只负责展示，部件层只负责接线。

## 1.0.37 重点

- 文件面板输入过滤时，搜索框尾部会继续显示命中数量，例如“3 / 120”，清除按钮仍保留在右侧。
- 用户边输入边判断结果规模，决定继续收窄、全选当前结果或直接引用，不必先扫列表估算命中数量。
- 清除过滤按钮的语义描述会带当前结果数量，组件层只负责展示，数量文案下沉到零件层。

## 1.0.36 重点

- 单文件引用后的 Toast 现在优先显示项目相对路径和大小，例如“已引用: app/src/README.md · 3 KB”。
- 当项目里有多个同名文件时，用户可以立刻确认刚引用的是哪一个路径，列表引用和预览引用都复用同一反馈。
- 相对路径仍复用文件引用提示的零件层路径规则，部件层只负责传入实际文件路径、项目根目录和大小。

## 1.0.35 重点

- 单文件引用后的 Toast 现在会显示文件名和大小，例如“已引用: README.md · 3 KB”。
- 从文件列表或预览弹窗引用单个文件时，反馈与批量引用保持一致，引用后能立即确认写入输入框的文件负载。
- 单文件引用反馈文案下沉到零件层，部件层只传入实际文件名和大小，避免在屏幕层散落重复文案。

## 1.0.34 重点

- 批量引用文件后的 Toast 现在会显示本次引用的总大小，例如“已引用 2 个文件 · 3 KB”。
- 分批引用匹配结果时，反馈会同时显示本次大小和剩余已选数量，处理大批文件时更容易确认进度和上下文负载。
- 引用反馈文案继续下沉到零件层，部件层只传入实际引用文件集合的大小，保持批量、匹配和当前可见引用链路一致。

## 1.0.33 重点

- 文件选择操作条在没有已选文件时，会显示当前可引用结果的总大小，例如“可选 5 个 · 3 KB”。
- 一键引用当前可见文件前，用户可以先判断上下文负载，避免把过大的搜索结果直接写入输入框。
- 文件选择大小汇总下沉到零件层，部件层只传入当前可见可引用文件集合，保持组件展示逻辑稳定。

## 1.0.32 重点

- 文件选择操作条在没有已选文件时，右侧按钮会变成可用的“引用 N”，可直接引用当前可见文件。
- 搜索过滤后可一步把当前结果写入输入框，不再需要先全选再引用，减少一次点击和一次状态切换。
- 当前可见引用的按钮标签和语义描述下沉到零件层，组件层只负责触发回调，部件层复用现有批量引用链路。

## 1.0.31 重点

- 文件选择操作条的“只看/全部”切换现在显示数量，例如“只看 7”“只看 3”“全部 11”。
- 有搜索词时，“只看”会显示当前匹配的已选数量；返回全部时显示当前过滤结果中的可选文件数量。
- 只看/全部切换标签规则下沉到零件层，组件层只负责渲染，部件层只传入当前过滤后的可选数量。

## 1.0.30 重点

- 文件选择操作条的图标按钮语义现在会带数量，例如“清空 7 个已选文件”“取消匹配 2 个已选文件”。
- 保持紧凑布局不变，同时让清空、取消可见、取消匹配和取消隐藏这些高风险动作更容易被系统读屏和长按语义识别。
- 图标动作描述规则下沉到零件层，组件层只负责接入，并补充四类纯函数测试。

## 1.0.29 重点

- 文件选择操作条的全选入口现在显示将新增的可见文件数量，例如“全选 7”或“补选 4”。
- 当过滤结果中已有部分文件被选中时，按钮会表达补选剩余数量，降低误以为重复选择全部文件的成本。
- 全选/补选数量标签规则下沉到零件层，组件层只负责渲染，并补充三类纯函数测试。

## 1.0.28 重点

- 文件选择操作条的引用按钮现在直接显示数量，例如“匹配 3”“全部 7”“引用 7”。
- 只看已选并搜索时，用户能在点击前确认将引用当前匹配子集还是全部已选，减少批量复核误操作。
- 引用按钮数量标签规则下沉到零件层，组件层只负责渲染，并补充四类纯函数测试。

## 1.0.27 重点

- 文件面板“引用匹配”处理完最后一批已选文件后，会自动收起文件面板并回到输入框。
- 仍有未引用选择时继续保持面板打开，分批处理不中断；最后一批完成时减少一次手动关闭操作。
- 匹配引用后的面板关闭规则下沉到零件层，部件层只负责调用关闭策略，并补充两类纯函数测试。

## 1.0.26 重点

- 文件面板分批“引用匹配”后，Toast 会显示已引用数量和剩余已选数量。
- 全量引用仍保持简洁提示，只有继续分批处理时才显示“剩余 N 个已选”，降低噪声。
- 批量引用提示文案下沉到零件层，部件层只传入引用数量和剩余数量，并补充两类纯函数测试。

## 1.0.25 重点

- 文件面板“引用匹配”后若仍有未引用的已选文件，会自动清空当前搜索词并回到剩余已选列表。
- 分批引用时不再停留在“匹配 0/N”的空结果状态，下一轮筛选可以直接从剩余集合继续。
- 过滤复位规则下沉到零件层，部件层只负责在匹配引用后接线，并补充三类纯函数测试。

## 1.0.24 重点

- 文件面板“引用匹配”后不再清空全部选择，只移除已经写入输入框的当前匹配项。
- 匹配引用会保持文件面板打开，未匹配的已选文件继续保留，便于按不同搜索词分批引用。
- 已引用路径移除规则下沉到零件层，部件层只负责关闭策略和状态接线，并补充目录忽略与保留未引用选择测试。

## 1.0.23 重点

- 文件面板“只看已选 + 搜索”且只命中部分已选文件时，新增“匹配”引用入口，可只把当前命中子集写入输入框。
- 主引用按钮在该场景切换为“全部”，明确区分引用当前匹配和引用全部已选，降低批量复核时误引用的风险。
- 引用匹配入口显示规则下沉到零件层，组件层只负责按钮语义和回调，并补充三类显示规则测试。

## 1.0.22 重点

- 文件面板“只看已选 + 搜索”时新增一键取消匹配选择，可快速从已选集合里移除当前命中项。
- 普通模式仍保留取消当前可见选择；只看已选但没有搜索词时不显示该入口，避免误清全部已选。
- 取消匹配入口显示规则下沉到零件层，组件层只负责按钮语义和回调，并补充三类显示规则测试。

## 1.0.21 重点

- 文件面板“只看已选 + 搜索”状态行改为显示“匹配 M/N”，直接表达当前已选集合中的命中数量。
- 搜索复核时不再把未命中的已选项写成“隐藏 X”，避免用户误以为有额外不可见文件会被批量引用。
- 状态文案规则保持在零件层，部件层只传入当前模式和搜索状态，并补充对应单元测试。

## 1.0.20 重点

- 文件面板“只看已选”模式现在会继续响应搜索词，复核大量已选文件时可二次缩小列表。
- 搜索摘要和实际显示列表保持一致，用户不需要退出只看已选再重新过滤。
- 当前显示列表规则下沉到零件层，覆盖普通过滤模式和只看已选过滤模式两类单元测试。

## 1.0.19 重点

- 文件面板搜索摘要固定使用真实过滤结果数量，不再被“只看已选”显示模式覆盖。
- 只看已选复核时，搜索框仍能准确显示当前查询命中数，用户不会误判过滤结果。
- 补充过滤零件层单元测试，覆盖名称、扩展名、路径匹配和摘要文案规则。

## 1.0.18 重点

- 文件面板在过滤结果为 0 但仍有已选文件时继续显示选择操作条。
- 用户可以直接看到“可见 0/0 · 隐藏 X”，并继续只看已选、清空、取消隐藏或批量引用。
- 操作条显示条件下沉到零件层，部件层只负责渲染接线，并补充无匹配隐藏选择的单元测试。

## 1.0.17 重点

- 文件面板选择操作条新增“取消隐藏选择”入口，过滤后可一键移除当前不可见的已选文件。
- 批量引用前可以先保留当前结果集的可见选择，清掉跨过滤条件残留选择，减少误引用。
- 隐藏选择清理规则下沉到零件层，组件层只暴露操作按钮，部件层只负责接线，并补充纯函数单元测试。

## 1.0.16 重点

- 文件面板选择状态新增“隐藏 X”提示，过滤后仍有不可见已选文件时会直接提醒。
- 状态行同时展示总已选、总大小、可见进度和隐藏已选数量，批量引用前更容易发现跨过滤条件的残留选择。
- 隐藏已选计算保持在零件层，组件层继续只展示状态标签，并补充隐藏和非隐藏两种单元测试。

## 1.0.15 重点

- 文件面板选中态新增“可见 M/N”进度，过滤后能立刻判断当前结果集是否已经选全。
- 状态行同时展示总已选数量、总大小和当前可见选择进度，批量引用前更容易发现漏选。
- 选择状态文案下沉到零件层，操作条只负责展示，补充有选择和无选择两种单元测试。

## 1.0.14 重点

- 文件面板选中态操作条改为“信息行 + 操作行”，已选数量和总大小在窄面板中稳定可读。
- 只看、清空、取消可见、全选和引用按钮集中在第二行，减少文字与按钮互相挤压。
- 操作条内部拆分状态文本和引用按钮小组件，组件层职责更清晰，未选择态仍保持紧凑一行。

## 1.0.13 重点

- 文件面板已选操作条新增已选文件总大小，批量引用前可快速判断上下文负载。
- 已选数量和大小在同一行展示，配合只看已选、全选可见和取消可见形成更稳的批量选择闭环。
- 已选大小汇总规则下沉到零件层，操作条只负责显示，并补充目录跳过与异常大小兜底测试。

## 1.0.12 重点

- 文件面板新增“取消当前可见选择”，过滤或排序后可一键撤销当前结果集里的已选文件。
- 批量选择、只看已选、取消可见和批量引用形成完整闭环，误选后无需清空全部或逐个取消。
- 可见选择删除规则下沉到零件层，操作入口保持在组件层，并补充跳过目录与保留不可见选择的单元测试。

## 1.0.11 重点

- 文件面板新增“只看已选”复核模式，批量引用前可快速检查当前已选文件集合。
- 已选操作条支持在“只看已选”和“全部结果”之间切换，仍可清空、继续全选可见或批量引用。
- 已选文件筛选规则下沉到零件层，操作条保持在组件层，并补充只返回已选文件的单元测试。

## 1.0.10 重点

- 文件面板新增“全选可见”，过滤和排序后可一键选择当前可见文件。
- 已选操作条在未选择时显示可选数量，选择后仍可继续补选当前可见结果并批量引用。
- 可见文件选择规则下沉到零件层，已选操作条保持在组件层，并补充跳过目录与去重测试。

## 1.0.9 重点

- 文件面板新增多文件选择，用户可一次勾选多个文件并批量引用到聊天输入。
- 批量引用会生成合并后的项目相对路径清单，减少多文件提问时重复点按和重复文案。
- 选择切换和批量提示下沉到零件层，已选操作条下沉到组件层，并补充对应单元测试。

## 1.0.8 重点

- 文件面板新增排序切换，可按名称、类型、大小和最近修改重排当前可见结果。
- 排序始终保持目录优先，过滤、摘要、排序和列表滚动之间的联动更自然。
- 排序规则下沉到零件层，排序切换条下沉到组件层，并补充零件层单元测试。

## 1.0.7 重点

- 文件面板新增可见结果结构摘要，快速显示目录数、文件数和当前可见文件总大小。
- 摘要条显示最多三个主要文件类型，过滤后会随结果同步变化，便于判断当前目录内容。
- 摘要计算下沉到零件层，摘要条下沉到组件层，聊天屏幕只负责组合过滤结果。

## 1.0.6 重点

- 文件面板新增当前目录快速过滤，可按文件名、扩展名或路径快速缩小列表。
- 过滤输入显示当前目录数量，输入后支持一键清除，便于继续浏览。
- 过滤能力下沉到零件层，搜索输入下沉到组件层，文件面板只负责组合和导航。

## 1.0.5 重点

- 长对话新增智能跟随滚动：在底部时继续跟随流式回复，上滑阅读历史时不再被强制拉回底部。
- 新增“到底部”浮动入口，阅读历史后可一键回到最新消息。
- 滚动判断下沉到零件层，浮动入口下沉到组件层，聊天屏幕只负责组合状态和用户意图。

## 1.0.4 重点

- 文件面板新增“引用到输入”，可一键把项目文件带入聊天草稿，减少手打路径和路径写错。
- 文件预览弹窗也支持引用，浏览、确认、提问之间的衔接更自然。
- 文件引用会自动转成项目相对路径，更贴合 Agent 文件工具的使用方式。

## 1.0.3 重点

- 聊天输入区新增命令快捷栏，常用 `/goal`、`/loop`、`/compact`、`/rewind`、`/model` 和停止任务入口可一键触达。
- 附件入口显示当前项目文件数量角标，减少打开文件面板前的信息盲区。
- 按零件/组件/部件分层补齐聊天快捷命令目录说明和更新日志。

## 1.0.2 重点

- 修复第三方 OpenAI 兼容服务的地址归一化与无鉴权网关支持。
- 第三方模型支持列表化管理，可新增、点选、删除，并可在主聊天页左上角快速切换。
- 设置页新增第三方模型测试诊断，可提示鉴权、路径、模型名、网络超时和 HTTP 私有网关问题。
- 修复流式工具调用的多工具 index 累积问题。
- 移除 API 客户端默认信任所有 TLS 证书的高风险行为。
- 持久化 `/goal` 会话目标，并修复 `/loop` 运行中无法稳定 `/stop` 的问题。
- Python 工具执行链调整为非隔离执行，设置页与命令帮助中移除沙箱开关。
- 对齐 Gradle wrapper 到本地已验证的 8.14.4。

## 许可证

原始项目版权归原作者 mzpr00 所有。如有任何问题请联系原作者或本仓库维护者。
